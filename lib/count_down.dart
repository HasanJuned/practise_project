import 'package:flutter/material.dart';

class AfterCompleteScreen extends StatelessWidget {
  const AfterCompleteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('After Complete Screen'),
      ),
    );
  }
}
